import { DepartmentNameDirective } from './department-name.directive';

describe('DepartmentNameDirective', () => {
  it('should create an instance', () => {
    const directive = new DepartmentNameDirective();
    expect(directive).toBeTruthy();
  });
});
