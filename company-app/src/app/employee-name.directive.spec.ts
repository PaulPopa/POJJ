import { EmployeeNameDirective } from './employee-name.directive';

describe('EmployeeNameDirective', () => {
  it('should create an instance', () => {
    const directive = new EmployeeNameDirective();
    expect(directive).toBeTruthy();
  });
});
