import { BankSortCodeDirective } from './bank-sort-code.directive';

describe('BankSortCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new BankSortCodeDirective();
    expect(directive).toBeTruthy();
  });
});
